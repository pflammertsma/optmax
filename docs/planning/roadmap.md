# Roadmap & Status

Living document — the single place to check what's built, in flight, and next.
Update when a phase lands or priorities change. (Last updated: 2026-07-18.)

## Done

### Phase 1 — Portfolio foundation
- [x] `lib/portfolio.js` — pure math: allocation, drift vs. targets, concentration, glidepath, CSV parsing (unit-tested)
- [x] IBKR **Activity Statement** parser: section-aware, per-lot data, FX conversion via the statement's own rates, cash from Cash Report, base-currency detection
- [x] Portfolio view: sortable holdings table (market value, weight, cost basis, P&L, currency, bucket, employer), cash as its own card, IBKR CSV import (annotations survive re-imports)
- [x] Targets view: bucket targets, tolerance, age-indexed recommendation, auto-assign buckets, drift table with dollar-specific trades
- [x] Guidance view: PFIC warnings (quote-backed with currency-validated classification), employer concentration alerts, cash drag, glidepath drift, Swiss professional-trader risk, lot-aware tax-loss harvesting and rebalance/LTCG warnings, ticker compliance lookup
- [x] `lib/health.js` — 100-point Portfolio Health grade (concentration 35 / diversification 25 / drift 15 / tax hygiene 15 / cash deployment 10) with grade caps (employer >25% ⇒ ≤C; PFIC present ⇒ ≤B), plain-English action per dimension
- [x] Health as a standalone page with inline grade badge in the sidebar
- [x] Health caching: SHA-1 fingerprint over score inputs + 24h TTL; recomputes only when the portfolio actually changes (`get-portfolio-health`, `force` option available)
- [x] Live price refresh: quantity × Yahoo quote, currency-guarded (mismatched exchange listings keep imported values), FX via `=X` pairs
- [x] Dividend income projection (trailing rates × quantity)
- [x] Privacy mode, employer symbols as durable store (`employerSymbols` survives imports)

## Outstanding — small items

- [x] **Screener showed the wrong data** — the "+ Add" field saves to the watchlist, but the Screener table was fed by the Discover market-scan cache (`screenerData`), so added symbols silently landed on the Dashboard instead, with no chip feedback (chip container was `display:none`). Fixed: the Screener table now reads the watchlist scan (`allData`, same source as the Dashboard), matching its "All watchlist stocks ranked by composite score" subtitle; Discover results still have their own dedicated table under Option Scanner. Watchlist is ~289 symbols (scanner universe), so instead of a chip wall: a "N stocks in your watchlist" count + on add, the new row scrolls into view and flashes with a success message (or a "hidden by filters" note). `removeFromWatchlist` now prunes `allData` in-memory so the table updates without a re-fetch.

- [x] **Health page self-sufficiency** — largely resolved in passing when `initPortfolioView()` gained a boot-time `renderPortfolio(getPortfolio())` (health loads at startup regardless of view); remaining gap closed 2026-07-18: clicking the Health nav now retries `get-portfolio-health` itself if the breakdown is empty (boot fetch failed or hasn't landed), so landing on it cold always works
- [ ] **Index-implied employer exposure** — count the employer's weight inside held index funds (QQQ/VOX) toward the concentration figure (needs fund-holdings data; Yahoo doesn't provide it directly)
- [ ] **Cost-drag health dimension** — weighted expense ratios via `quoteSummary`/`fundProfile`; deferred from the health scorer v1
- [x] **Metric-card overflow** — long values (e.g. `$1,854,155.22`) clip in the Total Value card
- [x] **Configurable concentration limit (2026-07-22)** — the single-stock/employer 10% ceiling was hardcoded in ~5 places (guidance warning + Trim rec, health action text, Progress target-line fallback, metric-card colors). Now one setting `concentrationLimitPct` (default 10, Settings → Long-Term Portfolio) drives all of them; the "critical" employer threshold derives as 1.5× the limit (10⇒15 preserves prior behavior). Health SCORE curve stays fixed (calibrated); only its action text uses the limit. Threaded via `settings`/opts into guidance.js + health.js, a `concentrationLimit` renderer mirror for colors + Progress fallback, and added to the health-cache fingerprint so the action text refreshes on change. Tests assert both the per-holding Trim and the employer warning respect a custom limit; verified in-app that the Progress target line follows the setting.
- [x] **Tax Profile card + PFIC relevance gate (2026-07-22)** — Settings → Tax Profile: residence/employment country, two citizenships, green-card checkbox, filing status, US marginal rate, IRS interest %, assumed-years fallback (all persisted). `lib/pfic.js` `isUSPerson()` now gates every PFIC surface (guidance warning, per-holding Replace rec, buy-idea exclusion/swaps, exit-cost estimator) so non-US persons see none of it. **Bug fixed same day:** the "PFIC applies / doesn't apply" banner read a settings object that could lack `citizenship1` (older settings files) while the dropdown showed "United States" via a `?? 'US'` display fallback — so a US citizen could see "doesn't apply." Now the banner computes from the **live dropdown selections** (`taxProfileIsUSPersonFromUI`), so it can never contradict what's shown. Country lists alphabetized; employment gained a "Not employed" option. US citizenship ⇒ PFIC always applies (worldwide taxation regardless of CH residence/employment).

## Outstanding — major phases

### Phase 2 — IBKR live sync (`lib/ibkr.js`) — IMPLEMENTED
Replace manual CSV import with the Client Portal Web API (local gateway
process, daily browser re-login). Plan in [`ibkr-integration.md`](ibkr-integration.md).
- [x] `lib/ibkr.js` — HTTPS client for the local Client Portal Gateway (self-signed cert), pure mapping functions unit-tested (17 tests): positions→holdings, ledger→cash, auth-status interpretation, pagination, injected FX
- [x] IPC: `ibkr-status`, `ibkr-sync`, `ibkr-open-login`; gateway URL in Settings; keep-alive tickle while authenticated
- [x] Sync merges into existing `portfolio.json` (source 'ibkr'), preserving buckets + employer flags; FX via Yahoo `=X` pairs (same source as price refresh); fingerprint invalidation applies automatically
- [x] Portfolio view: gateway status pill (connected / login-required / offline) + Sync IBKR button, re-probes on nav to Portfolio
- [x] **Convenience layer (2.5):** PortMax auto-launches the gateway (spawns `clientportal.gw` from a configured folder, tree-kills on quit, opt-in auto-start on boot) so no terminal is needed; **embedded login** via an in-app `<webview>` (no external browser); **scoped TLS bypass** — `app.on('certificate-error')` trusts ONLY the loopback gateway origin (`isLoopbackGatewayUrl`, unit-tested), so the self-signed localhost cert loads with no interstitial and nothing else is trusted. Settings: gateway-folder picker + auto-start toggle. Pill is context-aware: offline→start gateway, needs-login→embedded login, auto-closes on connect.
- Confirmed working against a **real IBKR gateway** (user's first live sync succeeded). Convenience layer verified against a mock gateway: embedded webview loads self-signed HTTPS with no cert prompt, and process spawn/tree-kill confirmed at the OS level.
- [x] **Real-gateway fixes (2.6):** a live gateway with no session answers **401 empty-body** — now mapped to `needs-login` (was misread as `unreachable`, which made auto-launch look hung); explicit `Content-Length: 0` on POSTs (some builds 411 otherwise); gateway stdout/stderr drained to `gateway.log` in userData (prevents 64KB-pipe boot deadlock + gives diagnostics); spawn via explicit `cmd.exe /c` (no `shell:true`/DEP0190); supervised startup reports early exits with exit code + log tail (`ibkr-gateway-log` IPC); on gateway-ready the embedded login opens automatically. **Username prefill** (`ibkrUsername` setting) injected into the login form — password deliberately never stored; persistent webview partition keeps IBKR session cookies.
- Verified against the REAL gateway: needs-login detection, embedded webview rendering IBKR's actual `sso/Login` page (no cert prompt), and username prefill landing in the real form.
- [x] **Success-page capture:** the gateway's terminal "Client login succeeds" page is detected inside the webview (body-text match, with the existing status poll as fallback) and auto-closes the modal + kicks off Sync immediately, instead of leaving raw gateway text on screen. Verified against a mock gateway serving that exact page.
- [x] **Encrypted credentials + auto-submit to 2FA:** username + password stored via Electron `safeStorage` (OS-backed: DPAPI/Keychain/libsecret — never plaintext in settings.json; verified the on-disk value is real ciphertext), routed through dedicated `ibkr-save/get/has/clear-credentials` IPC deliberately separate from the generic settings path. Login modal fills both fields and auto-submits, landing directly at the 2FA prompt — submits only when both fields are actually filled, never username-only. Settings UI: password field (write-only, clears itself after save) + Save/Clear + live encryption-status label.
- Verified: safeStorage round-trip via IPC (encrypt → has → decrypt-matches → clear), Settings UI save/clear flow, and the exact fill+auto-submit script proven correct against a mock page shaped like IBKR's real form (`#user_name` + password input) with submit intercepted client-side — deliberately did NOT fire even throwaway credentials at the real IBKR auth endpoint (blocked by the harness's own safety classifier: real-account lockout/fraud-flag risk from an unauthorized live auth attempt). The real 2FA-reaching flow needs the user's own end-to-end confirmation.
- [x] **First real-device confirmation (2026-07-16):** user confirmed the auto-fill + auto-submit flow works end-to-end and lands at the IBKR Mobile push 2FA screen against the real gateway.
- [x] **Fix: stuck on 2FA-approval screen.** After approving the IBKR Mobile push, the modal stayed on "Open the IBKR notification on your phone" indefinitely. Root cause: IBKR's SSO page updates itself in place after phone approval (no full page navigation), so the existing success-detector (`did-stop-loading` only) never re-fired. Fixed by adding a 1.5s body-text poll (`ibkrLoginDomPoll`) that runs for as long as the modal is open, independent of navigation events; the existing 3s gateway auth-status poll remains as a second fallback. Also added `did-fail-load`/`console-message` forwarding from the webview to devtools for diagnosing the `ERR_CERT_AUTHORITY_INVALID` (net_error -202) log the user saw — likely benign Chromium noise from the certificate-override retry (expected once per connection when a self-signed cert is trusted via `certificate-error`), but not yet confirmed since it wasn't isolated from the real stuck-screen bug. Needs the user's confirmation that the DOM-poll fix resolves the hang; if the SSL log recurs continuously (not just once), the new console forwarding will surface it for further diagnosis.
- [x] **Fix: spurious second login prompt right after 2FA.** After completing 2FA, the app briefly reopened the login modal a second time even though the user was already connected — `onIbkrLoginSuccess()` awaited a fresh `refreshIbkrStatus()` REST call before clicking Sync, and the real gateway's `/iserver/auth/status` can still answer `needs-login` for a moment right after 2FA approval (session propagation lag), so the Sync button's own click handler (which reads `ibkrState` synchronously) saw a stale `needs-login` and reopened the modal. Fixed by setting `ibkrState = 'connected'` directly (we already know this from the success signal that triggered the function) before clicking Sync, and moving the status refresh to a non-blocking background call after.
- [x] **Fix: genuinely stuck on 2FA screen after approving.** Distinct from the spurious-reprompt bug above — this was the case where the phone push arrived, was approved, and the modal never progressed at all. Root cause: `getStatus()` only called `POST /iserver/auth/status`, but the real gateway can keep answering `authenticated:false` on that endpoint alone for a while after 2FA approval — a known Client Portal Gateway quirk where the session needs a `/tickle` call to actually finalize into an authenticated state (previously `tickle()` existed but was only used for the post-connect keep-alive, never during the login-detection poll). Fixed in `lib/ibkr.js`'s `getStatus()`: tickle the session immediately before checking auth/status, every time. This benefits both the login modal's 3s status poll and the general status pill everywhere else.
- [x] **Fix: 2FA (phone push AND manual challenge code) consistently failing only through the embedded webview.** Diagnosed together with the user: username/password auth always succeeded and reached the 2FA screen, but neither the phone push nor typing the manual challenge code ever completed — `ibkrStatus()` kept reporting `needs-login` even after approving. Isolated by having the user log in through a plain external browser tab (`https://localhost:5000`, self-signed cert accepted manually) — full login incl. 2FA succeeded there every time. That proved it wasn't IBKR/account-side (ruled out the earlier suspicion of anti-fraud lockout from repeated attempts) and wasn't our auth-status polling — it was specific to the embedded `<webview>`'s networking. Root cause: `app.on('certificate-error')` only reliably covers the webview's top-level navigation request; it does not consistently cover every background XHR/fetch the IBKR login page itself fires while long-polling for phone-push approval or validating a challenge code. Those subresource requests hitting the same self-signed loopback origin were failing at the TLS layer (the repeating `ERR_CERT_AUTHORITY_INVALID` / net_error -202 log seen earlier was NOT benign noise as first assumed — it was the actual cause), so IBKR's own page-side JS never learned the 2FA had been approved. Fixed by additionally scoping `session.fromPartition('persist:ibkr').setCertificateVerifyProc()` — covers every request type uniformly for that dedicated partition, not just navigations.
- [x] **Password-storage warning + login modal visual polish:** Settings now shows an explicit amber warning next to the password field recommending against storing it (plaintext briefly held in memory in the auto-fill path) even though it's supported. Login modal restyled: centered spinner during load, white background matching IBKR's page (no dark flash), small branded header icon.
- [x] **Fix: stuck "disconnected" after approving the phone 2FA (2026-07-22).** User reported the gateway starting, the phone push arriving and being approved, yet the status pill staying disconnected. Root cause (distinct from all the webview-TLS/tickle fixes above): after 2FA approval the gateway's `/iserver/auth/status` reports `{ connected: true, authenticated: false }` — a live SSO session whose *brokerage* side isn't authenticated yet. `interpretAuthStatus` mapped any `authenticated:false` to `needs-login`, and `getStatus` only ever *tickled* (which keeps a session alive but never promotes an unauthenticated one). The gateway requires an explicit `POST /iserver/reauthenticate` to finish the handshake, which was never called — so the status was pinned at needs-login forever. Fixed in `lib/ibkr.js`: `getStatus` now detects `connected:true && !authenticated`, calls a new `reauthenticate()`, then re-polls auth/status a few times until it flips to `authenticated:true`. A genuine no-session state (`connected:false` / 401) is deliberately left alone so we don't spin reauth on a gateway the user hasn't logged into. Module-scoped `reauthInFlight` guard prevents overlapping status polls (login poll + status pill) from each firing their own reauth cycle; poll delay/attempts are injectable for fast tests. `interpretAuthStatus` now also returns `connected`. 5 new/updated unit tests (stateful mock: connected→reauth→authenticated; no-session skips reauth; never-promotes stays needs-login). **Verified against mocks only — needs the user's real-gateway confirmation that the pill now flips to connected after phone approval.** If it still hangs, the next diagnostic is to capture the raw `/iserver/auth/status` JSON post-approval (may need `/iserver/auth/ssodh/init` instead of `/reauthenticate` on some gateway builds).
- [ ] **Not yet:** account selection UI for multi-account logins (uses the first account); Flex Query fallback; readiness spinner while Java boots (currently a status poll)

### Phase 2.7 — Tax-aware buy recommendations — IMPLEMENTED
User request: "specific recommendations for what to buy that fulfill the investment strategy, factoring in tax implications (US + NL citizenship, CH residence)". Deliberately built as a **rules-based engine** (not stock-picking): it surfaces what the user's own targets + tax profile imply, with a visible not-financial-advice disclaimer.
- [x] `lib/recommendations.js` — `generateBuyRecommendations()`: curated US-domiciled candidate universe + existing holdings + watchlist A/B names; hard-excludes PFICs and employer stock; concentration guard (single stocks ≥8% weight excluded); ranks by bucket-underweight $, then **dividend tax drag** (yield × `dividendTaxRatePct`, default 30%), then fund-over-stock; glidepath overlay promotes bonds when equity exceeds the age-based target; greedy cash-aware `suggestedUsd` sizing from deployable cash (cash − buffer); per-candidate tax notes (CH dividends-as-income vs. tax-free capital gains, bond-interest CH taxation, NL 15% withholding on NL-source names like ASML, CH 35% reclaimable withholding); PFIC→US-equivalent swap map (IWDC→VT, IUSC→VB, NUCL→NLR, UMI→URNM, CSPX→VOO, …). Per-bucket display cap (4) so one large gap can't crowd out other buckets. 13 unit tests.
- [x] **Key tax insight encoded:** CH taxes dividends as ordinary income but not private capital gains — so every 1% of yield is a real recurring cost and low-yield growth exposure is structurally optimal; income products (JEPQ ~9% yield ≈ 2.7%/yr drag) stay in the universe so the penalty is visible rather than hidden.
- [x] **PFIC leak fixed** in `calculateHoldingRecommendation`: core-underweight PFIC holdings (IWDC/IUSC were in `PF_CORE_ETFS`) no longer get 'Buy' — they get a new 'Replace' rec + red badge.
- [x] IPC `get-buy-recommendations` (currency-validated quotes for holdings, USD-only quotes for curated candidates), `dividendTaxRatePct` setting (Settings → Long-Term Portfolio), "Tax-Smart Buy Ideas" card on the **Guidance view** (full-width atop the grid; user moved it off Portfolio, which stays table-only) with ranked ideas, strategy reason, tax notes, suggested $, per-idea tax-drag %, glidepath banner, PFIC-swaps section. Verified against the real portfolio via Playwright: bonds ranked first (96% equity vs. age-42 target 68%), AGG suggested ~$64.5k, 5 PFIC swaps listed.

### Phase 2.8 — Symbol Insights dialog + fund look-through — IMPLEMENTED
- [x] `lib/funds.js` — `computeFundOverlap()`: overlaps a fund's top holdings against the user's direct positions; merges share classes (GOOGL+GOOG via `canonicalSymbol` alias map) so hidden employer exposure is counted correctly; reports overlap count/%, employer % of fund, and $-per-$10k hidden employer exposure. 7 unit tests (test/funds.test.js, in npm test).
- [x] IPC `get-symbol-insights` — Yahoo `quoteSummary` (topHoldings, fundProfile, summaryDetail, assetProfile) with a 7-day file cache (`fund-insights-cache.json`; fund compositions change slowly), combined with quote data, `analyzeTicker` compliance, dividend tax drag at the user's rate, and the user's own position in the symbol.
- [x] **Symbol Insights dialog** (`#symbol-insight-overlay`) — separate from the CSP opportunity modal (which is options-trade-specific and would crash on funds): compliance banner (PFIC/suitability color-coded), key stats (yield, tax drag, expense ratio, 52-week position, fund assets/market cap, sector, own position), **fund top-10 look-through** with bars color-coded red (employer stock) / amber (already held directly) / cyan, an overlap callout ("X% of this fund is your employer's stock — every $10k adds ~$Y hidden exposure"), sector weight chips, and a deep-link button into the CSP modal when scanner data exists for the symbol.
- [x] Symbols in the Tax-Smart Buy Ideas card + PFIC swap rows are clickable (delegated handler, `.si-symbol-link`). Verified against real data via Playwright: VT shows 3.2% GOOG (≈$322 hidden per $10k), 7 of top-10 already held (17.7% of fund), 0.06% expense ratio, sector chips.
- Realizes the previously-deferred "Index-implied employer exposure" idea at the per-fund level.
- [x] **Portfolio-wide employer look-through aggregation:** `computeIndexImpliedEmployer()` in lib/funds.js (3 more tests) + IPC `get-employer-exposure` (quotes gate the quoteSummary calls to actual ETFs/mutual funds; 7-day cache) + the Portfolio "Employer Stock" metric card now shows `total% (direct% + via-funds%)` with a per-fund tooltip breakdown. Explicitly reported as a floor — only each fund's top ~10 holdings are visible. Real data: 38.5% direct + 1.64% via funds = **40.17% true GOOG exposure** ($28,964 hidden across 6 funds; VOX alone is 22.2% Alphabet). Health-score integration deliberately deferred (the concentration grade already caps at E; adding 1.6% wouldn't change behavior).
- [x] **Yield data fix (round 2):** Yahoo zeroes `trailingAnnualDividendYield`/`Rate` for many ETFs (IEFA, VT) while filling `dividendYield` (in percent, not fraction) — engine + insights IPC now read it as a third source. This was why IEFA briefly ranked as a "0.00% drag / minimal distributions" idea when it actually yields ~3.4%.

### Phase 3 — Background mode & notifications
Plan in [`architecture.md`](architecture.md) §1–2, §6; signal catalog in [`signals.md`](signals.md).
- [ ] System tray + keep-alive on window close (opt-in setting)
- [ ] Signal scheduler (hourly-to-daily): sync → recompute health/guidance → notify
- [ ] Native notifications with dedup (`signals.json` state log; fire on threshold *transitions* or a daily digest, not every poll)
- [ ] Notify on health **grade change** specifically

### Phase 4.5 — Dashboard Action Plan — IMPLEMENTED (2026-07-18)
Landing-page answer to "what should I do?": one prioritized, deduplicated to-do
list merged from every advice engine, shown as "Your Action Plan" atop the Dashboard.
- [x] `lib/actions.js` — `buildActionPlan()` (pure, 15 unit tests in npm test): merges sell-down plan status, tax-smart buy ideas, PFIC swaps, per-holding Buy/Trim/Replace recs, and compliance guidance into ≤7 ranked actions (sells → replacements → buys → review FYIs). Arbitration rules so the same problem never appears twice: an active sell-down plan suppresses the concentration alerts and the employer Trim rec; with no plan, employer weight >10% (candidate passed from the selldown logic — works even when the employer flag lives only on the holding, where guidance's settings-based alert never fires) becomes a "Start an employer-stock sell-down plan" action absorbing the Trim; PFIC swaps merge into one Replace row and suppress the grouped PFIC alert + per-holding Replace recs; cash-sized buy ideas (top 3 + overflow row) suppress the cash-drag lecture.
- [x] IPC `get-action-plan` (main.js): composes portfolio, cached quotes, `generatePortfolioGuidance`, `generateBuyRecommendations`, sell-down status, and `portfolioView` holding recs main-side.
- [x] Dashboard card replaces the old flat "Actionable Next Steps" list: numbered rows with kind badges (Sell amber / Replace red / Buy green / Review cyan), urgent flags, dollar amounts, click-to-navigate to the owning view; friendly "nothing needs your attention" empty state. Verified via Playwright smoke-test in both states (no plan ⇒ start-plan nudge first; active plan ⇒ "Sell ~12 shares of GOOG ≈ $4,153 · due by Oct 17" first).

### Phase 4.6 — Tax Profile + PFIC exit-cost estimator — IMPLEMENTED (2026-07-18)
Settings → Tax Profile: country of residence/employment, two citizenship slots, US
green-card checkbox (country selects populated from `TAX_COUNTRIES` in app.js), plus
estimator assumptions (US filing status, marginal rate, IRS interest %, assumed years
held fallback). A live status banner states whether US tax rules apply.
- [x] `lib/pfic.js` (pure, 10 unit tests): `isUSPerson(settings)` — US citizenship in either slot, US residence, or green card; an EMPTY profile deliberately counts as a US person (fail-safe: hiding PFIC warnings is the dangerous direction). `estimatePficExitCost()` models the default §1291 excess-distribution regime per lot: gain spread across calendar years held, prior-year slices at the top rate (37%) regardless of income + compounded interest at the assumed IRS underpayment rate, current-year slice at the user's marginal rate; `waitOneYearExtra` = same gain re-evaluated next year (zero-growth conservative); losses reported but never deducted; parses IBKR `YYYYMMDD` lot dates (fell back to assumed-age before that fix); no-lot-dates fallback flagged via `usedAssumedAge`.
- [x] **US-person gating** end-to-end: guidance PFIC group item, `calculateHoldingRecommendation` Replace rec (new `opts.usPerson` param, default true so existing tests/callers unchanged), recommendations PFIC exclusion + swaps, and the estimates IPC all switch off for non-US profiles. Verified in-app: flipping citizenship to NL/CH removes swaps, guidance items, Replace badges, and dashboard PFIC actions; restoring US brings them back.
- [x] IPC `get-pfic-estimates` + "What exiting is estimated to cost" table under the Guidance PFIC-swaps list: per fund — unrealized gain, est. tax today (with effective % of gain), the US-fund LTCG comparison, cost-of-waiting per year; totals line; assumptions + explicit not-tax-advice disclaimer pointing at Settings → Tax Profile. Real check on mock data: CSPX $500 gain held 3 calendar years ⇒ ~$192 (38% eff.) vs ~$75 in a US fund, +$10/yr to wait.
- Simplifications (documented in lib/pfic.js): equal per-year allocation (not day-weighted), flat interest assumption (real §6621 rates vary quarterly), no NIIT/state tax, no QEF/MTM election modeling.

### Phase 4.6 — Profile history & Progress view — IMPLEMENTED (2026-07-22)
Turns the app from "current snapshot only" into a trajectory. Before this, everything (portfolio.json, health-cache.json) was overwritten on each update — no history existed except `ivHistory` (options IV) and the sell-down baseline.
- [x] `lib/history.js` (pure, 13 tests): `parseStatementMeta(text)` pulls the statement `Period` date + NAV block (total/cash/stock/dividend-accruals/TWR) from an IBKR Activity Statement; `buildProfileSnapshot(opts)` builds a rich dated snapshot — structural metrics (value, cash%, buckets, employer direct%, top concentration, equity vs glidepath) computed from portfolio math with NO quotes (so backfill works), plus optional quote-dependent enrichments (health, dividends, employer-via-funds, PFIC value/count/symbols) that are null when absent (honest gaps, not fabricated); **retains a compact per-holding array per point** so future metrics can be recomputed over old history. `appendSnapshot` dedupes to one point/date (newer wins), keeps ascending order (backfilled older dates slot in), caps at 1000 points.
- [x] main.js: `profile-history.json` persistence; **capture hook on health recompute** (fingerprint-gated = only when the portfolio actually changed; reuses the quotes already fetched, no extra network); `seedProfileHistoryIfEmpty()` gives day-one a current point + the sell-down start point; IPC `get-profile-history` and `import-history-csv` (backfill a dated statement at its own date **without touching current holdings**, carrying over employer/bucket flags). PFIC/equity classification for snapshots works with quotes={} via analyzeTicker's known-symbol sets, so backfilled points still get PFIC exposure.
- [x] **Progress view** (new sidebar item): KPI row (employer concentration + delta since first point, health score + delta, total value, PFIC exposure); Chart.js line charts (bundled already) — employer concentration vs. the user's actual sell-down target line, health score 0–100, total value (privacy-masked axis). Import button for backfilling past statements. Empty-state + honest footnote about metric gaps.
- [x] **Backfilled the user's real July-14 Activity Statement**: history now shows employer concentration 42.58% (Jul 14) → 38.59% (Jul 18) → 38.21% (Jul 22), PFIC $33.5k→$32.0k. Verified the Progress view renders the trajectory via Playwright.
- Future: reconstruct more history from additional dated statements; add PFIC-exposure and equity-vs-target charts; drive Phase 3 notifications off history deltas (e.g. "health grade improved").

### Phase 4 — Plan-tracking features (turn advice into schedules)
- [x] **Systematic employer-stock sell-down plan — IMPLEMENTED (2026-07-18).** `lib/selldown.js` (pure math, 16 unit tests in npm test): `buildPlan()` snapshots the position and spreads the excess shares evenly over N quarters; `computePlanStatus()` judges ahead/on-track/behind against shares actually sold (exact across price swings) while recomputing the live goalposts (current weight, shares still to sell) from today's price — so a rally raises the tranche, a crash can complete the plan without selling, and new RSU vests are detected (`positionGrew`) and folded into the catch-up amount without flagging "behind" mid-quarter. Plan persists as `sellDownPlan` in portfolio.json; IPC `selldown-status` / `selldown-save-plan` / `selldown-clear-plan` (main.js). The status handler prices the position live (currency-validated quote, stored-value fallback) and re-bases the portfolio total on that same live value so the weight math is self-consistent. UI: full-width card atop Guidance — setup state explains concentration risk in plain English with a live "≈ N shares (~$X) per quarter" preview (target % + 4/6/8/12-quarter pace); active state shows a status chip, "This quarter's step: sell ~N shares before {date}", start→now→goal progress bar, sold/remaining stats, vest warning, and a lot-selection tax note (prefer >1yr, highest-cost lots; CH doesn't tax the gains); complete state congratulates and offers dismiss. Progress updates automatically on every IBKR sync. Verified end-to-end via Playwright against mock userData (`--smoke-test`): setup → save → active card → persistence → delete.
- [ ] **RSU vest log + reminders** — log vest dates/share counts (stock-plan broker has no public API; manual entry), remind at vest to sell/transfer, re-check concentration after
- [ ] **PFIC remediation checklist** — track replacement of flagged foreign-domiciled funds with US-domiciled equivalents

## Decisions still open (need user input)

- Phase order: 2 (live sync) vs. 3 (background/notifications) vs. 4 (sell-down plan) first
- Notification granularity: per-signal immediate vs. daily digest
- Gateway workflow acceptance: background Java process + ~daily browser re-login
- Bond/fixed-income introduction timing (glidepath currently reports drift only)
